apiVersion: v1
kind: Service
metadata:
  name: mongodb-svc
  namespace: dip-ns
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
  clusterIP: None
